name: pocketfft
description: Native AOT FFI bindings for high-performance mixed-radix Fast Fourier Transforms.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  ffi: ^2.1.2
  hooks: ^1.0.3
  code_assets: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  ffigen: ^20.1.1
  archive: ^3.6.0

name: resource_scope
description: Zone-based automatic scoped resource management for deterministic FFI and native memory disposal in Dart.
version: 0.0.1
resolution: workspace
repository: https://github.com/sigurdm/math

environment:
  sdk: ^3.11.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
